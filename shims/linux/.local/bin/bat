#!/bin/sh
# Shim: prefer upstream bat binary, fall back to Debian/Ubuntu batcat
if [ -x /usr/bin/bat ]; then
  exec /usr/bin/bat "$@"
fi

if [ -x /usr/local/bin/bat ]; then
  exec /usr/local/bin/bat "$@"
fi

exec batcat "$@"
