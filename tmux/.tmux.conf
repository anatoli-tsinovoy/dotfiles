
# Make tmux advertise a sane terminal to apps inside panes
set -g default-terminal "screen-256color"

# (optional but commonly needed) enable truecolor for modern apps
# tmux >= 3.2:
set -as terminal-features ",screen-256color:RGB"

set -g history-limit 200000
set -g mouse on
